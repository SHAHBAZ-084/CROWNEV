#!/bin/sh
# Remove Cursor co-author trailer so GitHub only credits your account.
# Enabled via: git config core.hooksPath .githooks
node "$(dirname "$0")/../scripts/strip-cursor-coauthor.mjs" "$1" 2>/dev/null || true
